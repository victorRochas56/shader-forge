// TODO
list of meshes + preview image (like unity / unreal) in imgui window
    ==> how to render preview of mesh?
    ==> how to draw a bunch of thumbnails to imgui

write shader
slang reflection -> generate struct for PC & shaderData
declare pipeline category in the shader -> create and assign right pipeline
shader ordering?